#include<stdio.h>

int main(){
	//Variable declaration
	int a,b;
	//input
	scanf("%d %d",&a,&b);
	//output paying close attention to the formatting specified in the problem (Print the variable SOMA with all the capital letters, 
	//with a blank space before and after the equal signal followed by the corresponding value to the sum of A and B).
	printf("SOMA = %d\n",a+b);
}
